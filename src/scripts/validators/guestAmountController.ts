// const increaseGuestAmount = (guestAmount: number, capacity: number, cb: () => void) => {
//   if (guestAmount >= capacity) {
//     alert("You have reached the maximum amount of guests for this room");
//     cb(capacity);
//   } else {
//     cb((prev) => prev + 1);
//   }
// };
// const decreaseGuestAmount = (guestAmount: number, cb: () => void) => {
//   if (guestAmount <= 0) {
//     alert("You cannot have less than 0 guests");
//     cb(0);
//   } else {
//     cb((prev) => prev - 1);
//   }
// };

// export { increaseGuestAmount, decreaseGuestAmount };
