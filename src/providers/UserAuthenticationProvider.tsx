import React from "react";

interface Props {
  children?: React.ReactNode;
}

function UserAuthenticationProvider({ children }: Props) {
  return <div>UserAuthenticationProvider</div>;
}

export default UserAuthenticationProvider;
